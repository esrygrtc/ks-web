Your agents never start over.
KeepState runs your AI agents inside snapshot-able micro-VMs. One API: run, checkpoint, fork, resume, replay. Any model, any harness.
An agent that dies at hour five is a bill. An agent that resumes is an employee.
Backed by builders. Run by design partners.
Agents die mid-task. You pay twice.
where the 6-hour run dies
of the token bill is replay
Sandbox crash, rate limit, provider blip. Everything gone. Restart from zero.
The same context, re-sent to the model step after step after step.
Framework, sandbox, workflow engine, state store, per-vendor caches.
Every serious agent team rebuilds this privately. None of them wants to.
One API. Autosave for AI agents.
Point any agent at KeepState. Same models, same code.
Filesystem + model memory + tool state. One atomic snapshot.
Branch step 40 into six universes. Keep the winner.
Warm resume under 100 ms. Cold under 90 s. On any provider.
Re-execute what actually happened. Evidence, not logs.
Three states. One freeze.
Kill it anywhere. Resume anywhere. The world and the memory can never disagree.
Claude Code's own resume is a quest log: your notes survive, the world resets. KeepState is a save state: the running tests, the dev server, the half-finished thought, frozen mid-frame and thawed intact.
Every checkpoint is a door to six futures.
Fork a two-day-old freeze ten times. The API is stateless; each fork is just a conversation that went differently. Your bill is the only one who notices.
The token bill drops the week you switch.
cost on the identical trace, measured, not vibed.
Cache-aware routing across providers.
Stored-response replay: never pay twice for an interrupted call.
Budget caps: ten forks can't detonate your card.
Logs tell you what the agent said it did. Replay re-executes what it actually did.
Deterministic replay from checkpointed state. Audit trails with sequence lineage. Deposition-grade evidence for the compliance officer, zero extra work for the engineer.
Enterprise: sovereign deploys, retention, SSO.
Counting the OOMs of agent-hours.
Agent task horizons keep doubling. Minutes became hours. Hours become days.
Inference-time compute now outweighs everything else in the agent economy.
MCP standardized tools. Long tasks became the default, not the demo.
What the 2024 forecasts called unhobbling is now a workload. Its substrate is unowned.
fal taxes generative media. KeepState taxes agent-hours.
Every long-running task in the world is becoming a session: metered, durable, forkable, replayable. We are building the layer that keeps that state, the way the caravanserai kept the caravans: the place where long journeys checkpoint for the night and continue at dawn. The open session protocol is named han in its honor.
Send us one trace of your longest run.
