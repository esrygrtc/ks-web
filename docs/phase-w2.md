# Phase W2 - Marketing pages

HYPOTHESIS: the public site matches its spec and its laws. Four house
lints green, sync-claims hash check green, footer honest, sitemap +
OG + robots sane.

## Built (19 pages)

Home, four product pages (one shared template, per-product data),
Pricing (NaN-proof ESTIMATED calculator, server-rendered defaults),
Proof + Compatibility (generated by scripts/sync-claims from the
product repo: 16 claims, 2 matrix rows, 11 tags, source de08d81f),
Customers (labeled placeholders, no fake logos), Enterprise, Security,
Grants (form UI, backend W4), Careers, Manifesto, Status (all
unavailable-by-honesty), About, Press, Contact, design-system.

## What the composites carried and the site refused

Fictional gate tags (G-101..G-171) replaced by the real nine; sample
uptimes (99.98%) and five invented incidents replaced by honest
unavailable states; five invented grant recipients replaced by an
explicit empty placeholder; a five-logo design-partner strip dropped;
invented pilot reports (PR-001..004) shipped as labeled template
placeholders; 'pen test completed 2026-05' and 'SOC 2 window closes
Q4 2026' downgraded to not-yet-published/not-yet-claimed until the
founder supplies records. Demo numbers (41.2M tokens, $4,100, sub-
100ms resumes) replaced with proof.json-backed measurements, badged
and evidence-linked, throughout.

## For founder content review (Section E moment)

- security@keepstate.ai appears as the disclosure address; mail
  routing must exist before cutover (BACKLOG W-1).
- Security/Enterprise certification rows read not-yet; restore real
  dates only with records in hand.
- Careers lists engineer #1 as genuinely open with salary terms.

## Fixes en route (all disclosed before gate re-runs)

- lint-copy extended to subdirectories before gate-w2's first run,
  so product pages carry fidelity contracts (139 lines held total).
- check-sync.mjs used process.exit(1), which skips finally and
  stranded proof.json.bak during the first sabotage run; now sets
  exitCode so the restore always runs. Both modes re-run after.

## Gate W2 - normal run (raw output, exit 0)

```
=== GATE W2 (normal mode) 2026-08-19T19:33:02Z ===
--- 1. sync freshness and the four lints ---
check-sync: proof.json matches a fresh generation
ASSERT PASS: sync-claims hash check green
ASSERT PASS: em-dash lint green
ASSERT PASS: evidence lint green
--- 2. build, then post-build lints ---
ASSERT PASS: site builds
link lint: clean (19 pages, no broken links, no orphans)
ASSERT PASS: link lint green (no broken links, no orphans)
copy lint: clean (139 verbatim lines held)
ASSERT PASS: copy fidelity green
--- 3. the footer is honest ---
ASSERT PASS: footer status chip renders unavailable with no feed
ASSERT PASS: system line prints unavailable per field, never zero
ASSERT PASS: no fake green anywhere in the footer
--- 4. sitemap, OG, robots ---
ASSERT PASS: sitemap generated
ASSERT PASS: sitemap excludes internal tooling
ASSERT PASS: OG image present and non-trivial
ASSERT PASS: robots.txt sane, cites the sitemap
ASSERT PASS: page inventory: 19 pages built
GATE W2: GREEN - the public site matches its spec and its laws
```

## Gate W2 - sabotage run (raw output, exit 0)

```
=== GATE W2 (--sabotage mode) 2026-08-19T19:33:03Z ===
--- sabotage 1: an em dash in content must fail the em-dash lint ---
ASSERT PASS: em-dash lint fails on the planted em dash
--- sabotage 2: an evidence-free claim row must fail the evidence lint AND the hash check ---
ASSERT PASS: evidence lint fails on the evidence-free row
ASSERT PASS: hash check fails on the hand edit (UI Law 4 bites)
ASSERT PASS: restored; evidence lint green again
GATE W2 SABOTAGE: GREEN - the lints bite
```
